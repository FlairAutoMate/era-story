{
  "name": "era-story",
  "version": "1.0.0",
  "private": true,
  "description": "ERA Scroll Story — static landing page with a serverless lead intake",
  "type": "module",
  "dependencies": {
    "@vercel/blob": "^2.8.0"
  },
  "scripts": {
    "leads": "node leads.mjs",
    "leads:csv": "node leads.mjs --csv",
    "qa": "node qa-story.mjs",
    "dashboard:dev": "npm --prefix dashboard run dev",
    "dashboard:build": "npm --prefix dashboard run build",
    "dashboard:test": "npm --prefix dashboard run test",
    "dashboard:e2e": "npm --prefix dashboard run e2e"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
